1、支持邮箱对接，支持邮箱找回密码，支持全站管理员重置用户密码后发送邮件让用户重置密码。
2、支持单页导出功能：
    1、支持导出markdown原文，图片处理为完整URL，附件也需要处理为完整的URL，不能使用相对路径。
    2、导出PDF功能，使用浏览器的PDF打印能力，需要保持阅读区的预览格式，不能包含左侧文档树。

3、增加文档模板功能，用户可以在编辑区选择模版，全站管理员可以在后台配置场景模板。
我发现，现在系统配置的全文检索配置没有开关，我希望加一个开关，开启了则前端显示输入框，后端可以配置权限检索项，同时启动搜索引擎的索引重建。如果没有开启全文检索，则不需要浪费资源建立索引。